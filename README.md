# Drought-predictor-project



### steps I followed
1- pipenv install -r requirements.txt
2- pipenv shell
3- jupyter notebook -> Model training
4- I am running with different data fractions, because the training takes long.
5- Check the variables in mlflow with: pipenv run mlflow ui

